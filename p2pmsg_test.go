package main

/*

  WHY ARE THERE NO TESTS YET?
	Because I forked a working example without tests, then modified it to my liking.

	ARE YOU GOING TO WRITE TESTS?
	Sure. Probably when I'll add functionality

*/

import (
	"testing"
)

/*
- No remote data? We create first data
- Remote data? We create our own and send it to the remote data

*/

func TestHelloWorld(t *testing.T) {
	// Check we have remote data

	// Check we create our own first data

	// Check we can wait when we have no remote data

	// Check we send our data over to the other user
}
